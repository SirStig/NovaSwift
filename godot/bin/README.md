The GDExtension libraries (libNovaSwiftGodot.so / .dll / .dylib) are built into this directory by scripts/build-gdextension.sh.
They are git-ignored; only this file is tracked so the directory exists.
